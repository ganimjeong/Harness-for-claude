---
description: Run the harness check script (lint + typecheck + test, when available)
---

Run `scripts/check` from the repo root and report:

1. Whether it succeeded.
2. Any failing lint, typecheck, or test output.
3. If nothing ran, say so — the harness is stack-aware and skips silently.
