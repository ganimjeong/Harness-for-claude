---
description: Run the harness bootstrap script to install dependencies
---

Run `scripts/bootstrap` from the repo root. Report which stack it detected
(npm / pnpm / yarn / uv / pip / cargo) and whether install succeeded.
